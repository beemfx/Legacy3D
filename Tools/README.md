# Tools

## LPaKager.exe
This is a tool for viewing .lpk files. Unfortunately the source code for this
was lost, so I'm including the binary here for the history. Apparently it
requirest lf_sys2.dll which doesn't exist (but I think it could be built from
the lf_sys2 directoy).